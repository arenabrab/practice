spring:
  application:
    name: metrics
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    password: password
  jpa:
    hibernate:
      ddl-auto: validate
  r2dbc:
    url: r2dbc:postgresql://localhost:5432/postgres
    password: password